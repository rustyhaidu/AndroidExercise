<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".activitati.GraphActivity"></activity>
        <activity android:name=".activitati.TransferValoare" />
        <activity android:name=".activitati.ImageActivity" />
        <activity android:name=".activitati.AfisareGoluri" />
        <activity android:name=".activitati.AdaugareGolActivity" />
        <activity android:name=".activitati.AfisareEchipePrezente" />
        <activity android:name=".activitati.AdaugareEchipa" />
        <activity android:name=".activitati.HomeActivity" />
        <activity android:name=".activitati.JsonActivity" />
        <activity android:name=".activitati.AfisareMeciuri" />
        <activity android:name=".activitati.AdaugareMeci" />
        <activity android:name=".activitati.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>